#' Calcular el Coeficiente de Gini
#'
#' Esta función calcula el coeficiente de Gini para medir la desigualdad o
#' concentración de una distribución (muy útil para modelos de riesgo o análisis de remesas).
#'
#' @param x Un vector numérico con los datos (por ejemplo, ingresos o montos de transacciones).
#'
#' @return Un valor numérico entre 0 y 1 que representa el coeficiente de Gini.
#'
#' @examples
#' ingresos <- c(100, 200, 300, 400, 1000)
#' calcular_gini(ingresos)
#'
#' @export
calcular_gini <- function(x) {
  # (Aquí iría tu código real del cálculo)
  print("¡Hola! Esta es mi primera función documentada 2.")
}
